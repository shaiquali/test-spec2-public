spec_version: 2
description: deploy s3 bucket

outputs:
  bucket_1:
    value: '{{ .grains.bucket_1.outputs.s3_bucket_arn }}'

inputs:
  compute_service_name:
    type: string
    default: "eks"

grains:
  bucket_1:
    kind: terraform
    spec: 
      source:
        store: spec2_repo_blue
        path: terraform/s3
      agent:
        name: '{{.inputs.compute_service_name}}'
      inputs:
        - region: eu-west-1
        - acl: private
        - name: 'test-bucket-1-s3-{{ sandboxid | downcase }}'
        - tag1: 'value_from_blue'
        - 'param_tag': '{{ .params.sen }}'
      outputs:
        - s3_bucket_arn
      scripts: 
        pre-tf-init:
          source:
             store: spec2_repo_blue
             path : scripts/echo.sh
        pre-tf-destroy:
          source:
            store: spec2_repo_blue
            path : scripts/echo2.sh

