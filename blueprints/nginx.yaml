spec_version: 2
description: Blueprint for nginx helm chart 

grains:

  nginx:
    kind: helm
    spec:
      source:
        store: spec2_repo_blue
        path: helm/nginx
      host:
        name: aks
      commands:
        - dep up helm/nginx
